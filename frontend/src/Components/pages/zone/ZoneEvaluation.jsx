import React from 'react';
import Layout from '../../layout/Layout';

const ZoneEvaluation = () => {
    const user = JSON.parse(localStorage.getItem('user'));

    return (
        <Layout>
            <h1>Zone Evaluation Page </h1>
            
        </Layout>
    );
};

export default ZoneEvaluation;
