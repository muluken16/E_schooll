import React from 'react';
import Layout from '../../layout/Layout';

const TeacherStaffOversite = () => {
    const user = JSON.parse(localStorage.getItem('user'));

    return (
        <Layout>
            <h1>Teacher and Staff Oversite for Zone </h1>
           
        </Layout>
    );
};

export default TeacherStaffOversite;
