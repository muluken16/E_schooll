import React from 'react';
import Layout from '../../layout/Layout';

const Policy = () => {
    const user = JSON.parse(localStorage.getItem('user'));

    return (
        <Layout>
            <h1>Zone Policy Update for wereda educations and schools </h1>
           
        </Layout>
    );
};

export default Policy;
