import React from 'react';
import Layout from '../../layout/Layout';

const SchoolInfrastructure = () => {
    const user = JSON.parse(localStorage.getItem('user'));

    return (
        <Layout>
            <h1>School Infrastructure by Zone </h1>
           
        </Layout>
    );
};

export default SchoolInfrastructure;
